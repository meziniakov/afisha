import { FC } from "react";
import Filters from "./Filters";
import Search from "./Search";
import styles from "./SearchSection.module.scss";

const SearchSection = () => {
  return <section className={styles.section}>{/* <Search /> */}</section>;
};

export default SearchSection;
